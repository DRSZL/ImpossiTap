# Home Screen Glow — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Subtiler Neon-Glow-Look auf dem Home Screen durch sechs visuelle Anpassungen in einer einzigen Datei.

**Architecture:** Alle Änderungen in `flutter/lib/screens/home_screen.dart`. Keine Logikänderungen, keine neuen Dateien. Die Hintergrund-Aura hinter dem Play-Button wird via `OverflowBox` + `Stack` realisiert, damit der Glow das Layout nicht beeinflusst. Alle anderen Änderungen sind reine `TextStyle`- und `BoxDecoration`-Swaps.

**Tech Stack:** Flutter, dart:ui (Shadow/BoxShadow — keine neuen Dependencies)

---

## Dateiübersicht

| Datei | Aktion |
|-------|--------|
| `flutter/lib/screens/home_screen.dart` | Modify |

---

### Task 1: Home Screen Glow

**Files:**
- Modify: `flutter/lib/screens/home_screen.dart`

- [ ] **Step 1: Hintergrundfarbe + Subtexte abdunkeln**

In `_HomeScreenState.build()` den Scaffold-Hintergrund ändern:

```dart
// vorher
backgroundColor: const Color(0xFF0A0A0A),

// nachher
backgroundColor: const Color(0xFF080808),
```

Die drei dekorativen Subtexte von `0xFF555555` auf `0xFF383838` setzen. Betrifft genau diese drei `TextStyle`-Instanzen:

```dart
// Tagline: "YOU CAN'T. BUT TRY ANYWAY."
style: const TextStyle(
  fontFamily: 'DMMono',
  fontSize: 10,
  color: Color(0xFF383838),   // war 0xFF555555
  letterSpacing: 3,
),

// Label: "HEUTIGES ZIEL"
style: const TextStyle(
  fontFamily: 'DMMono',
  fontSize: 10,
  color: Color(0xFF383838),   // war 0xFF555555
  letterSpacing: 4,
),

// Unit: "MIKROSEKUNDEN"
style: const TextStyle(
  fontFamily: 'DMMono',
  fontSize: 11,
  color: Color(0xFF383838),   // war 0xFF555555
  letterSpacing: 3,
),
```

Der inaktive Nav-Tab bleibt bei `0xFF555555` — nur die drei oben genannten ändern sich.

- [ ] **Step 2: Zielzahl — Text-Glow**

Dem `Text(GameState.formatUs(GameState.targetUs), ...)` die `shadows`-Property hinzufügen:

```dart
Text(
  GameState.formatUs(GameState.targetUs),
  style: const TextStyle(
    fontFamily: 'BebasNeue',
    fontSize: 46,
    color: Color(0xFFC8F55A),
    letterSpacing: 1,
    height: 1,
    shadows: [
      Shadow(color: Color(0x47C8F55A), blurRadius: 20),
      Shadow(color: Color(0x1AC8F55A), blurRadius: 50),
    ],
  ),
),
```

`0x47` = 28% Opacity, `0x1A` = 10% Opacity (ARGB-Format: Alpha zuerst).

- [ ] **Step 3: Play-Button — 4-Layer boxShadow + Hintergrund-Aura**

Den gesamten `GestureDetector`-Block (Play-Button) ersetzen. Die `OverflowBox` lässt die Aura-Kreise über die 180×180 Button-Bounds hinausragen, ohne das Column-Layout zu verschieben:

```dart
GestureDetector(
  onTap: _goToGame,
  child: AnimatedBuilder(
    animation: _pulseAnim,
    builder: (context, child) {
      return SizedBox(
        width: 180,
        height: 180,
        child: OverflowBox(
          maxWidth: 340,
          maxHeight: 340,
          child: Stack(
            alignment: Alignment.center,
            children: [
              // Äußere diffuse Aura
              Container(
                width: 340,
                height: 340,
                decoration: const BoxDecoration(
                  shape: BoxShape.circle,
                  gradient: RadialGradient(
                    colors: [Color(0x0AC8F55A), Colors.transparent],
                  ),
                ),
              ),
              // Innere engere Aura
              Container(
                width: 260,
                height: 260,
                decoration: const BoxDecoration(
                  shape: BoxShape.circle,
                  gradient: RadialGradient(
                    colors: [Color(0x06C8F55A), Colors.transparent],
                  ),
                ),
              ),
              // Play-Button
              Container(
                width: 180,
                height: 180,
                decoration: BoxDecoration(
                  shape: BoxShape.circle,
                  color: const Color(0xFFC8F55A),
                  boxShadow: [
                    BoxShadow(
                      color: const Color(0xFFC8F55A).withOpacity(0.12),
                      blurRadius: 0,
                      spreadRadius: 1,
                    ),
                    BoxShadow(
                      color: const Color(0xFFC8F55A)
                          .withOpacity(_pulseAnim.value * 0.55),
                      blurRadius: 25,
                    ),
                    BoxShadow(
                      color: const Color(0xFFC8F55A)
                          .withOpacity(_pulseAnim.value * 0.25),
                      blurRadius: 60,
                    ),
                    BoxShadow(
                      color: const Color(0xFFC8F55A)
                          .withOpacity(_pulseAnim.value * 0.12),
                      blurRadius: 110,
                    ),
                  ],
                ),
                child: const Column(
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    Text(
                      'TIPPE',
                      style: TextStyle(
                        fontFamily: 'BebasNeue',
                        fontSize: 28,
                        color: Color(0xFF0A0A0A),
                        letterSpacing: 4,
                      ),
                    ),
                    Text(
                      'ZWEIMAL',
                      style: TextStyle(
                        fontFamily: 'DMMono',
                        fontSize: 9,
                        color: Color(0x80000000),
                        letterSpacing: 2,
                      ),
                    ),
                  ],
                ),
              ),
            ],
          ),
        ),
      );
    },
  ),
),
```

`0x0A` = ~4% Opacity, `0x06` = ~2.4% Opacity.

- [ ] **Step 4: `_StatBox` — Lime-Tint**

In der `_StatBox`-Klasse (am Ende der Datei) `build()` komplett ersetzen:

```dart
@override
Widget build(BuildContext context) {
  return Container(
    padding: const EdgeInsets.symmetric(horizontal: 18, vertical: 14),
    decoration: BoxDecoration(
      color: const Color(0x04C8F55A),
      border: Border.all(color: const Color(0x0FC8F55A)),
    ),
    child: Column(
      children: [
        Text(
          value,
          style: const TextStyle(
            fontFamily: 'DMMono',
            fontSize: 14,
            color: Colors.white,
            fontWeight: FontWeight.w500,
            shadows: [Shadow(color: Color(0x2EC8F55A), blurRadius: 8)],
          ),
        ),
        const SizedBox(height: 4),
        Text(
          label,
          style: const TextStyle(
            fontFamily: 'DMMono',
            fontSize: 8,
            color: Color(0xFF555555),
            letterSpacing: 2,
          ),
        ),
      ],
    ),
  );
}
```

`0x04` = 1.5% Opacity, `0x0F` = 6% Opacity, `0x2E` = 18% Opacity.

- [ ] **Step 5: `_NavTab` — aktiver Tab Glow**

In der `_NavTab`-Klasse das `Text`-Widget anpassen:

```dart
child: Text(
  label,
  style: TextStyle(
    fontFamily: 'DMMono',
    fontSize: 10,
    letterSpacing: 2,
    color: active ? const Color(0xFFC8F55A) : const Color(0xFF555555),
    shadows: active
        ? [const Shadow(color: Color(0x33C8F55A), blurRadius: 6)]
        : null,
  ),
),
```

`0x33` = 20% Opacity.

- [ ] **Step 6: Tests ausführen — erwartet: PASS**

```bash
cd flutter && flutter test
```

Erwartet: All tests pass. Visuelle Überprüfung auf Gerät/Emulator empfohlen — Glows wirken auf OLED-Displays intensiver als auf LCD.

- [ ] **Step 7: Commit**

```bash
git add flutter/lib/screens/home_screen.dart
git commit -m "Apply glow visual polish to home screen"
```
